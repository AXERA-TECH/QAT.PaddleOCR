name: ppocrv6-small-det-float-finetune
task: det
model_name: PP-OCRv6_small_det
qat: false
training:
  epochs: 100
  batch_size: 8
  workers: 8
  optimizer: Adam
  learning_rate: 0.0001
  weight_decay: 0.00001
  image_shape: [3, 640, 640]
  augmentation: paddle
  amp: false
  warmup_epochs: 2
  lr_final_factor: 0.1
  save_every: 1
